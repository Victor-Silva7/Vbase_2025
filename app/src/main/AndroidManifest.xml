<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for camera and storage -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
        android:maxSdkVersion="28" />
    
    <!-- Camera feature (optional) -->
    <uses-feature 
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name=".VGroupApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidAppTemplate"
        tools:targetApi="31">
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="" />
        
        <!-- Add RegistroPlantaActivity -->
        <activity 
            android:name=".ui.registro.RegistroPlantaActivity"
            android:theme="@style/Theme.AndroidAppTemplate.NoActionBar"
            android:windowSoftInputMode="adjustResize"
            android:exported="false" />
        
        <!-- Add RegistroInsetoActivity -->
        <activity 
            android:name=".ui.registro.RegistroInsetoActivity"
            android:theme="@style/Theme.AndroidAppTemplate.NoActionBar"
            android:windowSoftInputMode="adjustResize"
            android:exported="false"
            android:windowActionBar="false"
            android:windowNoTitle="true" />
        
        <!-- Add AiLogicActivity -->
        <activity 
            android:name=".ui.ai.AiLogicActivity"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.AndroidAppTemplate" />
        
        <!-- Comentarios Activity -->
        <activity
            android:name=".ui.comentarios.ComentariosActivity"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.AndroidAppTemplate" />
        
        <!-- Meus Registros Activity (fallback se navegação falhar) -->
        <activity
            android:name=".ui.registro.MeusRegistrosActivity"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.AndroidAppTemplate" />
        
        <!-- FileProvider for camera images -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>