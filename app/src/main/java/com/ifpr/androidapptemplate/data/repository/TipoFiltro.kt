package com.ifpr.androidapptemplate.data.repository

/**
 * Enum para tipos de filtro no feed de postagens
 */
enum class TipoFiltro {
    TODAS,      // Todas as postagens
    PLANTAS,    // Apenas postagens de plantas
    INSETOS     // Apenas postagens de insetos
}